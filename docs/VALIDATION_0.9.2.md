# Validação 0.9.2

Antes do merge, o PR deve passar o workflow padrão com frontend, Rust fmt/check/test, process harness, Android e pacote Windows. Após o merge, a mesma matriz deve passar em `main` e o workflow de release deve publicar a versão 0.9.2.

A validação em máquina real deve confirmar: artwork do ZZZ após novo sync Steam, importação de conta com API key do usuário e respostas das fontes de preço configuradas.

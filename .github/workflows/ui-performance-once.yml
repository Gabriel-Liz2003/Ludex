name: ui-performance-helper-disabled

on:
  workflow_dispatch:

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "UI performance helper is disabled; an earlier queued run may still apply the versioned patch."

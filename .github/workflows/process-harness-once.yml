name: process-harness-helper-disabled

on:
  workflow_dispatch:

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Process harness application helper is disabled; permanent CI owns validation."
